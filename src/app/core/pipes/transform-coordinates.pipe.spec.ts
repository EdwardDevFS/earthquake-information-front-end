import { TransformCoordinatesPipe } from './transform-coordinates.pipe';

describe('TransformCoordinatesPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformCoordinatesPipe();
    expect(pipe).toBeTruthy();
  });
});
